//string,number,boolean,void,any

const username: string = "zekaryas";
const age: number = 24; //number can be integer, float ,decimal
const isAlive: boolean = true;

// any can have any datatype set to it
const place: any = "12";


//void can have only null or undefined value
const work: void = null;
const work2: void = undefined;


//to check datatype of a variable(any)
if (typeof place == 'string') {
    //it is string
}
